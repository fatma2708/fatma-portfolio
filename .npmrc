# This project pins React 16 (CRA 5). Several transitive dependencies
# declare newer optional peers (e.g. lenis optional peer react>=17).
# Relaxing peer resolution keeps `npm ci` reproducible on CI and clean
# machines without affecting the production bundle.
legacy-peer-deps=true
